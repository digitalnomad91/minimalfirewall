<Page
    x:Class="MinimalFirewall.Pages.LiveConnectionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header + controls -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Live TCP Connections"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center" />
            <Button x:Name="RefreshButton" Grid.Column="1"
                    Margin="0,0,8,0"
                    Click="RefreshButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;" FontSize="14" />
                    <TextBlock Text="Refresh" />
                </StackPanel>
            </Button>
            <ProgressRing x:Name="RefreshSpinner" Grid.Column="2"
                          Width="24" Height="24"
                          IsActive="False" />
        </Grid>

        <!-- Disabled overlay -->
        <InfoBar x:Name="DisabledInfoBar"
                 Grid.Row="1"
                 IsOpen="False"
                 Severity="Informational"
                 Title="Traffic Monitor is disabled"
                 Message="Enable it in Settings to view live connections."
                 IsClosable="False"
                 Margin="0,0,0,8" />

        <!-- Connections grid -->
        <controls:DataGrid
            x:Name="LiveDataGrid"
            Grid.Row="2"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            GridLinesVisibility="Horizontal"
            CanUserResizeColumns="True"
            CanUserSortColumns="True"
            SelectionMode="Extended"
            Sorting="LiveDataGrid_Sorting">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="140" />
                <controls:DataGridTextColumn Header="PID" Binding="{Binding Connection.ProcessId}" Width="60" />
                <controls:DataGridTextColumn Header="Local Address" Binding="{Binding LocalAddress}" Width="150" />
                <controls:DataGridTextColumn Header="Local Port" Binding="{Binding LocalPort}" Width="90" />
                <controls:DataGridTextColumn Header="Remote Address" Binding="{Binding RemoteAddress}" Width="150" />
                <controls:DataGridTextColumn Header="Remote Port" Binding="{Binding RemotePort}" Width="90" />
                <controls:DataGridTextColumn Header="State" Binding="{Binding State}" Width="100" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <!-- Status -->
        <TextBlock x:Name="LiveStatusText" Grid.Row="3"
                   Margin="0,4,0,0" FontSize="11"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </Grid>
</Page>
