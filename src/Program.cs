namespace MinimalFirewall
{
    // Entry point delegates to App.Main() for WinUI 3
    internal static class Program
    {
        [System.STAThread]
        static void Main(string[] args) => App.Main(args);
    }
}
